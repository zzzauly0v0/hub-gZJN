##修改前
---
name: markdown-table-align
description: 生成中英文混排(CJK)时源码级对齐正确的 Markdown 表格。当用户要求生成、格式化、修复 Markdown 表格,尤其是表格内容同时含中文和英文/数字、导致源码里(纯文本/等宽字体环境下)看起来没对齐时,必须使用此 skill。也适用于把整齐的表格写入 .md 笔记文件、GitHub README 等场景。用户说"表格没对齐"、"错位"、"帮我生成一个表格"且内容含中文时都应触发。
---

# Markdown 表格中英混排对齐

## 背景

Markdown 渲染器本身不关心源码里每个单元格前后有多少空格——渲染后会自动按最长内容对齐一列。但当在**纯文本/等宽字体环境**(VSCode 编辑 .md 源码、终端、Typora 源码模式)里查看未渲染的表格源码时,常常会对不齐,例如:

```
| 姓名   | Score |
|--------|-------|
| 张三   | 95    |
| Alice  | 100   |
```

原因是中文字符(CJK)在等宽字体下通常占 2 个字符宽度,英文字母/数字占 1 个宽度。如果按"字符数"简单填充空格,中文越多的单元格看起来越短、越不对齐。

## 核心算法

用 Unicode East Asian Width 属性判断每个字符的显示宽度:
- `unicodedata.east_asian_width(ch)` 返回 `'W'`(Wide)或 `'F'`(Fullwidth)的字符,按宽度 2 计算
- 其余(`'Na'`、`'H'`、`'A'`、`'N'`)按宽度 1 计算

每列宽度 = 该列所有单元格(含表头)的最大**显示宽度**。填充空格补齐到这个宽度,而不是补齐到字符数。

## 使用方法

优先使用 `scripts/align_table.py`,不要手动数中英文混排的宽度——很容易算错。

```bash
python3 scripts/align_table.py input.json
```

输入 JSON 格式(第一行是表头):
```json
[
  ["姓名", "Score", "备注"],
  ["张三", "95", "优秀"],
  ["Alice Wang", "100", "Perfect score"]
]
```

也支持:
- CSV 输入:加 `--csv` 参数
- stdin 输入:路径传 `-`
- 每列对齐方式:`--align left,right,center`(逗号分隔,不填默认全部左对齐)

如果用户是在对话里直接给了一份数据(表格、列表、JSON),先手动把它转成上面的 JSON 结构存成临时文件,再调用脚本,不要自己手动拼空格对齐。

## 何时使用这个 skill

- 用户要求"生成一个 Markdown 表格"且内容包含中文
- 用户反馈表格"没对齐"、"错位",要求修复
- 需要把表格写入 .md 笔记文件,且用户可能在源码/等宽字体环境下查看
- 用户提供数据(表格、JSON、CSV、列表)要求转换成整齐的 Markdown 表格

## 注意事项

- 脚本会自动让分隔行 `|---|---|` 的横线数量跟该列宽度一致,不需要手动调整。
- 单元格内容如果很长或包含换行,先跟用户确认要不要截断——本 skill 默认不做截断处理。
- 结果要直接写入目标 .md 文件(用 str_replace/create_file),不要只在对话里打印,除非用户就是想先预览一下再决定。
##修改后
---
name: markdown-table-align
description: 生成中英文混排的 Markdown 表格并对齐。含中文内容的表格必须调用脚本计算,禁止手动逐字符估算宽度。
---
# Markdown 表格中英混排对齐

中文字符按宽度 2、其余按宽度 1 计算，每列补齐到该列最大宽度。

**必须调用脚本，不要手动计算：**
```bash
python3 scripts/align_table.py input.json --align left,right,center
```
输入：JSON 二维数组，第一行为表头；也支持 `--csv`、`-`(stdin)。

流程：数据 → 存为临时 JSON/CSV → 调脚本 → 结果写入目标 .md 文件（用 str_replace/create_file）。不在对话里手动拼接空格。

跳过对齐：若表格只在会自动渲染的地方展示（聊天框、GitHub 页面），不需要执行以上流程，直接输出普通表格。
