一、MiniMax-M3

基本信息：MiniMax 于 2026 年 6 月发布，428B 总参数 / 约 23B 激活参数，60 层 Decoder-only Transformer，原生多模态（图像+视频），上下文 1M token。是在上一代 MiniMax-M2.7（229B 稠密/MoE 文本模型）基础上的三项结构性升级之一。

注意力路线：MSA（MiniMax Sparse Attention） M3 放弃了自己上一代 M1 押注的 Lightning Attention（线性注意力），转而走"稀疏化标准 attention"路线。核心设计是把 attention 计算拆成两步：

一个轻量的 Index Branch（每层仅新增约 390 万参数，占总参数量 0.001%）先对 Key 按 128-token 为一个 block 做打分；
主干的 Q/K/V/O 投影完全不变（仍是标准 GQA，64 个 query head，num_key_value_heads=4），只是每个 query 只和 top-k 相关的 block（加一个恒定可见的本地窗口 block）做真正的 attention 计算。

这种设计被称为"架构上的免费午餐"——不改变 Transformer 骨干本身，只在 attention 内部插入一个索引分支。60 层中只有前 3 层保留标准全量 attention（其余 57 层用 MSA），原因是浅层的 token 表示还没有充分分化，这时做稀疏索引选择并不可靠，需要靠全量 attention 打好基础表示后，后面的层才能安全地做稀疏化。此外每个 attention head 都带 QK-Norm，RoPE 只作用在 head_dim 的前一部分（partial RoPE）。

官方给出的效率数据：相比 M2 在 1M 上下文下，MSA 带来约 9 倍的 prefill 加速和 15 倍的 decode 加速，单 token 计算量降到约 1/20（社区对代码的逐行拆解给出的 decode FLOPs 降幅约 30 倍）。

FFN / MoE 延续 M2 系列的 sigmoid 路由（而非业界更常见的 softmax）。选择 sigmoid 的原因是它允许多个专家同时打出独立的高分，这对训练中的负载均衡更灵活；负载均衡效果则通过一个可学习的辅助 bias 校正项来实现，起到类似 softmax 天然归一化的作用。专家池规模为 128 个专家，每个 token 激活 top-4 路由专家 + 1 个恒定激活的共享专家。

残差连接：标准残差 + Norm，没有引入类似 DeepSeek V4 mHC 那样的多路残差设计。

位置编码：Partial RoPE（只覆盖 head_dim 的一部分），配合 attention 内部的 QK-Norm。

MTP：checkpoint 中带有 7 个独立的 MTP 模块（num_mtp_modules=7），直接接在最后一层 hidden state 上用于原生投机解码。但截至目前公开的 Transformers Python 实现里并未包含 MTP 的前向计算逻辑——权重虽然以 mtp.* 前缀存在于 checkpoint 里，标准 from_pretrained 加载时会通过 _keys_to_ignore_on_load_unexpected 直接跳过，模型退化为普通逐 token 自回归生成，MTP 需要专门的推理框架（如官方 vLLM/SGLang 适配）才能启用。

精度：官方发布 BF16 与 MXFP8 两个版本的 checkpoint。

这个模型的关键启发：MSA 证明了"改 attention 但不动 Q/K/V/O 投影本身，只加一个廉价的索引分支"是可行的稀疏化路径，和 DeepSeek 的 MLA（压缩 KV 表征）、Kimi K3 的 KDA（换成线性注意力）是三条完全不同的技术路线，但目标都是解决同一个问题：1M 级上下文下的 attention 计算/显存爆炸。

二、LongCat-Flash（美团）

基本信息：美团 LongCat 团队 2025 年 8 月发布，560B 总参数，MoE 架构，实际每 token 激活参数在 18.6B～31.3B 之间动态浮动，平均约 27B。这个"激活参数量会变"本身就是 LongCat-Flash 最大的架构特点，来自其核心创新——零计算专家。

FFN / MoE：Zero-Computation Experts + ScMoE LongCat-Flash 的出发点是"不是所有 token 都值得同样的计算量"。标准 MoE 里每个 token 无论难易都固定路由到 K 个 FFN 专家；LongCat-Flash 在专家池里额外混入了一批零计算专家（本质是恒等函数，token 过这个"专家"等于什么都不做，直接透传），路由器会给简单 token（比如标点符号）分配零计算专家，把计算预算留给真正需要深度处理的复杂 token。为了让平均激活参数量保持稳定（不因为路由器行为漂移而失控），系统用一个 PID 控制器动态调整专家偏置项，把平均激活量控制在约 27B 附近。

第二项创新是 ScMoE（Shortcut-connected MoE）：把某一层 FFN/MoE 的计算和下一层的通信做重叠调度，扩大"计算-通信重叠窗口"，这是训练/推理工程效率上的设计，不改变数学上的模型行为，但让千卡级 MoE 训练和高吞吐推理成为可能。官方数据：20 万亿+ token 的训练在 30 天内完成，推理侧单卡吞吐超过 100 token/s（H800），输出成本约 0.7 美元/百万 token。

注意力路线：采用改进版 MLA（Multi-head Latent Attention），在标准 MLA 基础上引入了 scale-correction 因子（αq、αkv）来解决大规模训练中的方差失配问题——技术报告指出，模块间的方差失配是导致模型规模扩大后训练不稳定、性能衰减的关键因素之一，因此他们专门做了"方差对齐"改造，而不是直接照搬 DeepSeek 的 MLA 公式。

MTP（投机解码）：LongCat-Flash 用 MTP 做 draft 模型，但和大多数用 MoE 结构做 MTP 头不同，它的 MTP 头是单个稠密（dense）层而非 MoE 层——这是刻意的设计取舍，稠密 MTP 头在 draft 延迟和接受率之间取得更好平衡，实测接受率超过 90%。

精度/工程侧：论文强调了"SwapAB" GEMM（小 batch 场景下把权重当作矩阵乘法的左手矩阵以提升 Tensor Core 利用率）和自定义通信 kernel（用 multimem.st/multimem.ld_reduce 替代 NCCL），这些属于系统工程优化，不属于模型结构本身，但是理解"MoE 模型为什么能又大又快"绕不开的部分。

后续演进（LongCat-2.0，2026 年 7 月）：同一团队把 LongCat-Flash 的路线继续往前推了一步，做到 1.6T 参数、原生 1M 上下文，核心新增是 LongCat Sparse Attention (LSA)——官方将其定位为 DeepSeek Sparse Attention 的演进版本，叠加了三种正交的索引机制：流式感知索引（把碎片化内存读取变成连续块读取）、跨层索引（相邻层复用注意力显著性分数，不用每层重新计算）、分层索引（先粗筛后精筛的两阶段过滤）。同时新增了一个 1350 亿参数的 N-gram Embedding 模块，与 MoE 专家正交存在，用来捕捉稠密的局部 token 关系，同时降低大 batch 解码时的显存 I/O 压力。这部分因为是 7 月刚发布，公开的源码/config 细节还不如 LongCat-Flash 本身完整，本文只做路线记录，不展开细节分析。

这个模型的关键启发：LongCat-Flash 是目前几家里唯一明确把"token 难度不均等"写进架构设计的模型——GLM/DeepSeek/Kimi 的稀疏化都是在"选哪些 KV 值得看"上做文章，LongCat 是在"这个 token 值不值得算"上做文章，两者是正交的优化方向。

三、ERNIE 4.5（百度）

基本信息：百度 2025 年 6 月开源，家族里有 10 个变体，涵盖纯文本 LLM（如 ERNIE-4.5-300B-A47B，300B 总参 / 47B 激活）和视觉-语言 VLM（如 ERNIE-4.5-VL-424B-A47B，424B 总参 / 47B 激活）。三家里唯一一个把"多模态"直接设计进 MoE 路由本身、而不是像 Kimi K3 那样用独立 vision tower 外挂的模型。

FFN / MoE：异构 MoE + 模态隔离路由 这是 ERNIE 4.5 最核心的结构创新。同一个 MoE 层里的专家被分成不同的"模态组"：文本 token 只能被路由到文本专家组，图像 token 只能被路由到视觉专家组，两组专家在参数上是分开的（不共享），但 self-attention 参数和一部分专家（共享专家）在模态间是共享的。为了让训练不出现"一个模态学习挤占另一个模态"的问题，团队额外引入了两个正则项：router 正交损失（约束不同模态路由方向不冲突）和多模态 token 均衡损失（保证两种模态的 token 在训练中都被充分利用）。

一个很具体的工程细节：视觉 token 天然存在大量冗余信息，所以视觉专家组的 FFN 中间维度只有文本专家组的 1/3，由于 FFN 的 FLOPs 与"输入维度 × 中间维度"成正比，这一项设计单独就带来了视觉 token 前向计算约 66% 的 FLOPs 削减。另外，模型的最后一个 Transformer 层直接去掉了视觉专家——报告给出的理由是最后一层视觉专家的权重对最终输出的贡献可以忽略,直接砍掉换取效率。

因为文本模型和视觉-语言模型的参数是按模态分开统计的,同一"级别"的 LLM 版本比 VLM 版本总参数更小(比如 300B vs 424B),这也是为什么表里看到同一系列会有两个不同的总参数数字。

残差/位置编码/精度:技术报告的重点全部放在 MoE 路由和多模态训练策略上,没有像 DeepSeek V4(mHC)或 Kimi K3(AttnRes)那样在残差连接上做特殊改动;位置编码沿用标准 RoPE。训练侧用 FP8 混合精度;推理侧提出了"卷积编码量化算法"用来做 4-bit/2-bit 的无损量化,并配合 PD 分离(prefill/decode 分离部署,支持动态角色切换)提升推理吞吐——这些同样是工程优化而非模型结构本身。ERNIE 4.5 是用百度自研的 PaddlePaddle 框架训练和部署的,不在 HuggingFace Transformers 生态原生跑,这点和其余几家都不一样。

这个模型的关键启发:同样是做多模态,Kimi K3 走的是"文本骨干 + 外挂 vision tower + LLaVA 式融合"路线(vision 和文本用完全独立的模块,只在 embedding 层面拼接),ERNIE 4.5 走的是"多模态从 MoE 路由层面就深度耦合"路线(vision/text 共享 attention,但专家分组隔离)。这是两种理念上完全不同的多模态接入方式,值得在作业里作为对比案例。
