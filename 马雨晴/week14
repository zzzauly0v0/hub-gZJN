Flash Card：英语单词闪卡生成

目标

根据用户指定的一个英语单词，生成结构化 JSON 数据，并调用本 skill 自带脚本生成可直接打开的静态 HTML 学习闪卡。

卡片内容顺序固定为：

单词与 IPA 音标

词性与中文释义

近义词

3 条中英对照例句

适用场景

用户明确要求为某个英语单词制作闪卡时使用，例如：

“给我做一张 resilient 的单词闪卡”

“生成 meticulous 的 flash card”

“帮我制作 crazy 的背词卡”

“Make a vocabulary flash card for pragmatic.”

不适用场景

以下请求不要触发本 skill：

仅询问单词释义、音标、用法或翻译，但没有要求生成闪卡

要求生成句子练习、测验、词汇表或 Anki 卡组

一次要求处理多个单词，且没有明确要求逐个生成独立闪卡

目标不是英语单词，例如中文成语、整段文本或专有名词介绍

如果用户一次提供多个单词，先请用户确认要处理的单词，或在用户明确要求批量生成时逐词执行本流程，每个单词生成独立 JSON 和 HTML 文件。

目录约定

以当前 SKILL.md 所在目录作为 SKILL_DIR，不要硬编码 .cursor/skills/... 等绝对或项目相关路径。

目录结构应为：

flash_card/
├── SKILL.md
├── data/
└── script/
    └── make_flashcard.py

文件位置：

JSON 数据：<SKILL_DIR>/data/<slug>.json

生成脚本：<SKILL_DIR>/script/make_flashcard.py

HTML 输出：当前工作目录下的 ./<slug>.html

执行流程

1. 提取目标单词

从用户请求中提取一个目标英语单词。

处理规则：

word 字段保留正确拼写

文件名使用小写安全标识 slug

slug 仅保留字母、数字、连字符和下划线

空格替换为下划线

不允许路径分隔符、.. 或其他可能导致路径穿越的内容

如果没有识别到明确单词，只询问一次：

请告诉我需要制作闪卡的英语单词。

2. 生成学习数据

生成 UTF-8 编码的 JSON 文件，字段必须严格符合以下结构：

{
  "word": "resilient",
  "phonetic": "/rɪˈzɪliənt/",
  "pos": "adj.",
  "definition": "有韧性的；能从困难或挫折中迅速恢复的",
  "examples": [
    {
      "en": "She remained resilient despite repeated setbacks.",
      "zh": "尽管屡遭挫折，她依然表现得很坚韧。"
    },
    {
      "en": "The local economy proved resilient during the crisis.",
      "zh": "当地经济在危机期间表现出了较强的韧性。"
    },
    {
      "en": "Resilient materials return to their original shape after pressure is removed.",
      "zh": "有弹性的材料在压力解除后会恢复原状。"
    }
  ],
  "synonyms": ["tough", "hardy", "adaptable", "durable", "robust"]
}

3. 内容质量要求

音标

使用 IPA，并用 /.../ 包裹

采用常见、规范的英式或美式发音之一

不确定时不要编造罕见音标形式

词性

使用标准缩写，例如：

n.

v.

adj.

adv.

prep.

conj.

若单词有多个常见词性，优先选择与当前释义和例句一致的主要词性，不要把多个无关义项混在同一张卡片中。

中文释义

简洁、准确，建议 15–40 个汉字

聚焦一个核心义项

与近义词和全部例句保持语义一致

不写冗长词典解释、词源或冷僻义项

例句

examples 必须恰好包含 3 条，每条必须包含：

en：自然、完整的英文句子

zh：与英文语义一致的中文翻译

质量要求：

每条英文例句都应实际使用目标单词或其合理词形变化

三条例句覆盖不同语境，避免只替换主语或宾语

每条英文建议 8–20 个单词

避免生僻专名、敏感内容和过度复杂的从句

不使用占位文本，例如 Example sentence 1

近义词

提供 4–6 个

仅选择与当前核心义项接近的词

不混入反义词、派生词或重复词

全部使用小写，专有名词除外

4. 保存并校验 JSON

将 JSON 保存到：

<SKILL_DIR>/data/<slug>.json

保存要求：

UTF-8 编码

中文直接保存，不转义为 \uXXXX

文件结尾保留换行

在调用脚本前检查：

所有必需字段均存在且非空

examples 长度严格等于 3

每条例句同时包含 en 和 zh

synonyms 长度为 4–6

JSON 能够被正常解析

校验失败时，先修正 JSON，再运行脚本；不要依赖脚本自动补齐占位内容。

5. 生成 HTML

从任意工作目录执行时，都应使用 SKILL_DIR 解析脚本与数据路径。

推荐命令：

python "<SKILL_DIR>/script/make_flashcard.py" \
  "<SKILL_DIR>/data/<slug>.json" \
  -o "./<slug>.html"

执行要求：

所有路径均使用引号包裹，兼容包含空格的 Windows 路径

HTML 输出到当前工作目录

同名文件已存在时可覆盖，除非用户明确要求保留旧版本

运行失败时，返回实际错误信息并指出失败步骤，不要声称文件已经生成

6. 返回结果

生成成功后，向用户说明：

已生成闪卡

JSON 数据文件路径

HTML 文件路径

在支持图形界面且用户明确要求预览时，可尝试使用默认浏览器打开 HTML；否则只返回文件路径，不强制启动浏览器。

推荐回复格式：

已生成 resilient 的单词闪卡：
- 数据：<SKILL_DIR>/data/resilient.json
- 闪卡：./resilient.html

异常处理

未提供单词：询问目标英语单词

脚本不存在：指出缺失的脚本路径并停止执行

JSON 不合法：修正数据后重新生成

脚本执行失败：保留错误输出，说明失败原因和对应文件路径

输出文件未生成：检查进程退出码和目标路径，不得仅凭命令已执行就宣称成功

完成标准

只有同时满足以下条件，任务才算完成：

<SKILL_DIR>/data/<slug>.json 已创建且通过字段校验

当前工作目录中的 <slug>.html 已实际生成

返回给用户的文件路径与真实输出路径一致
