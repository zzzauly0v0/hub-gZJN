
# HY_V3整体架构仍是标准的 Decoder-only Transformer + MoE（混合专家）



## 位置编码，使用RoPE 相对位置编码
```python
def apply_rotary_pos_emb(q, k, cos, sin, unsqueeze_dim=1):

    cos = cos.unsqueeze(unsqueeze_dim)
    sin = sin.unsqueeze(unsqueeze_dim)
    q_embed = (q * cos) + (rotate_half(q) * sin)
    k_embed = (k * cos) + (rotate_half(k) * sin)
    return q_embed, k_embed
```



## 归一化层 LayerNorm：使用 RMSNorm 替换

不算均值（不中心化），只用均方根

```python
@use_kernel_forward_from_hub("RMSNorm")
class HYV3RMSNorm(nn.Module):
    def __init__(self, hidden_size, eps: float = 1e-6) -> None:
        """
        HYV3RMSNorm is equivalent to T5LayerNorm
        """
        super().__init__()
        self.weight = nn.Parameter(torch.ones(hidden_size))
        self.variance_epsilon = eps

    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        input_dtype = hidden_states.dtype
        hidden_states = hidden_states.to(torch.float32)
        variance = hidden_states.pow(2).mean(-1, keepdim=True)
        hidden_states = hidden_states * torch.rsqrt(variance + self.variance_epsilon)
        return self.weight * hidden_states.to(input_dtype)

    def extra_repr(self):
        return f"{tuple(self.weight.shape)}, eps={self.variance_epsilon}"
```


## KV 使用 GQA 的 KV 存储

```python
self.head_dim = getattr(config, "head_dim", config.hidden_size // config.num_attention_heads)
self.num_key_value_groups = config.num_attention_heads // config.num_key_value_heads
self.scaling = self.head_dim**-0.5
```


## MoE 混合专家

Sigmoid 路由器 + 共享专家

```python
router_logits = F.linear(hidden_states.float(), self.weight.float())
routing_weights = torch.sigmoid(router_logits)
```






| 维度   | 传统 Transformer        | HYV3                                 |
| ---- | --------------------- | ------------------------------------ |
| 位置编码 | 可学习 /sin‑cos          | RoPE（可配 base + 动态）                   |
| 归一化  | LayerNorm / Post‑norm | RMSNorm + Pre‑norm                   |
| 注意力  | MHA                   | GQA + QK‑Norm + 多后端                  |
| FFN  | 固定 MLP                | MoE（Sigmoid 路由 + 共享专家 + 路由缩放 + 修正偏置） |



# MiniMax-M3-VL（多模态视觉-语言模型

在标准 Transformer 骨架上同时引入了 稀疏/块稀疏注意力（Lightning Indexer）、MoE、Gemma 风格 RMSNorm、SwiGLU 变体、3D RoPE 视觉塔



## 注意力：GQA + 逐头 QK-Norm + Partial RoPE（文本塔）

```python
class MiniMaxM3VLAttention(nn.Module):
    """
    M3 attention: per-head Gemma QK-norm + partial RoPE, optionally sparse indexer selection which require position IDs.
    """

    def __init__(self, config: MiniMaxM3VLTextConfig, layer_idx: int):
        super().__init__()
        self.config = config
        self.layer_idx = layer_idx
        self.head_dim = getattr(config, "head_dim", config.hidden_size // config.num_attention_heads)
        self.num_key_value_groups = config.num_attention_heads // config.num_key_value_heads
        self.scaling = self.head_dim**-0.5
        self.attention_dropout = config.attention_dropout
        self.is_causal = True
        self.q_proj = nn.Linear(config.hidden_size, config.num_attention_heads * self.head_dim, bias=False)
        self.k_proj = nn.Linear(config.hidden_size, config.num_key_value_heads * self.head_dim, bias=False)
        self.v_proj = nn.Linear(config.hidden_size, config.num_key_value_heads * self.head_dim, bias=False)
        self.o_proj = nn.Linear(config.num_attention_heads * self.head_dim, config.hidden_size, bias=False)
        self.q_norm = MiniMaxM3VLRMSNorm(self.head_dim, eps=config.rms_norm_eps)
        self.k_norm = MiniMaxM3VLRMSNorm(self.head_dim, eps=config.rms_norm_eps)
        self.indexer = (
            MiniMaxM3VLIndexer(config, layer_idx) if config.layer_types[layer_idx] == "minimax_m3_sparse" else None
        )
```


## 位置编码：3D RoPE（视觉塔）

```python
        base = config.rope_parameters["rope_theta"]
        partial_rotary_factor = config.rope_parameters.get("partial_rotary_factor", 1.0)
        head_dim = getattr(config, "head_dim", None) or config.hidden_size // config.num_attention_heads
        dim = int(head_dim * partial_rotary_factor)
```



## 注意力 核心差异：Lightning Indexer 块稀疏注意力


```python
self.indexer = (
    MiniMaxM3VLIndexer(config, layer_idx) if config.layer_types[layer_idx] == "minimax_m3_sparse" else None
)
```

用一个小型 index_n_heads 头的点积分支，对 query 与所有 key 打分；
把 key 分成 index_block_size 大小的块，对每个块做 max-pool 得到块分数；
每个 query 只保留 top-index_topk_blocks 个关键块，外加当前 query 之前的 index_local_blocks 个局部块（总是可见）；
主注意力的 mask 由这些"被选中块"构成（block-sparse），从而把长序列上的注意力成本大幅降低。

选择发生在块粒度而非单个 key 粒度，且 indexer 无 value 投影、不产生自己的残差（纯选择分支，类似 DeepSeek-V4 的 indexer）：


## 归一化 ：Gemma 风格 RMSNorm

```python
class MiniMaxM3VLRMSNorm(nn.Module):
    """Gemma-style RMSNorm: normalizes in fp32 and scales by `weight + 1`."""

    def __init__(self, dim: int, eps: float = 1e-6):
        super().__init__()
        self.eps = eps
        self.weight = nn.Parameter(torch.zeros(dim))

    def _norm(self, x):
        return x * torch.rsqrt(x.pow(2).mean(-1, keepdim=True) + self.eps)

    def forward(self, x):
        output = self._norm(x.float())
        # Llama does x.to(float16) * w whilst MiniMaxM3VL is (x * w).to(float16)
        # See https://github.com/huggingface/transformers/pull/29402
        output = output * (1.0 + self.weight.float())
        return output.type_as(x)

    def extra_repr(self):
        return f"{tuple(self.weight.shape)}, eps={self.eps}"
```

这里 MiniMaxM3VLRMSNorm 是 Gemma 风格：权重初始化为 0，缩放用 weight + 1（而非直接乘 weight），且输出 (x_norm * (1+weight)).to(orig_dtype)：


## FFN：MoE + 共享专家 + SwiGLU 变体

MiniMaxM3VLDecoderLayer 中每层 FFN 按 mlp_layer_types 选择 dense 或 MoE：


```python
self.mlp = (
    MiniMaxM3VLSparseMoeBlock(config)
    if config.mlp_layer_types[layer_idx] == "sparse"
    else MiniMaxM3VLDenseMLP(config, intermediate_size=config.dense_intermediate_size)
)
```

路由：Sigmoid + 修正偏置（和 M2 一致）

```python
routing_weights = F.sigmoid(router_logits.float())
scores_for_choice = routing_weights + self.e_score_correction_bias
```

共享专家 + 路由缩放（和 HYV3 类似，但有 clamp）
MiniMaxM3VLSparseMoeBlock 同时有共享专家和路由专家，路由输出额外乘以 routed_scaling_factor：

```python
self.routed_scaling_factor = config.routed_scaling_factor
self.shared_experts = MiniMaxM3VLDenseMLP(...)

hidden_states = self.experts(hidden_states, selected_experts, routing_weights)
hidden_states = hidden_states * self.routed_scaling_factor
hidden_states = hidden_states + shared_output
```

SwiGLU 变体（带 clamp 限幅）
MiniMaxM3VLDenseMLP 与专家 _apply_gate 使用带 swiglu_alpha / swiglu_limit 的 SwiGLU，且对 gate/up 做 clamp 限制：

```python
gate = gate.clamp(max=self.swiglu_limit)
up = up.clamp(min=-self.swiglu_limit, max=self.swiglu_limit)
glu = gate * torch.sigmoid(gate * self.swiglu_alpha)
return self.down_proj((up + 1.0) * glu)
```


|维度|传统 Transformer|MiniMax‑M3‑VL|
|---|---|---|
|注意力|全量 MHA|GQA + 逐头 QK‑Norm + 块稀疏 Lightning Indexer（长序列降本）|
|位置编码|可学习 /sin‑cos|Partial RoPE（文本）；3D RoPE（视觉塔）|
|归一化|LayerNorm|Gemma 风格 RMSNorm（1+weight，0 初始化），Pre‑Norm|
|FFN|固定 MLP|MoE（Sigmoid 路由 + 共享专家 + 路由缩放）+ SwiGLU 变体（带 clamp）|
|模态|纯文本|视觉塔 + 投影器 + 占位符 scatter（多模态）|
|负载均衡|无 /aux loss|有 Switch 风格 aux loss（可选）|